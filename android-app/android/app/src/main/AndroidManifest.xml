<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <queries>
        <package android:name="com.bmc.potholequickfix" />
        <package android:name="com.sis.pwdsewaapp" />
        <package android:name="com.kmc.app" />
        <package android:name="com.newnmmc.app" />
        <package android:name="com.nyatitechnologies.pmcroadmitra" />
        <package android:name="com.ceedeev.grivenancev2" />
        <package android:name="org.tnega.cmhelpline.citizen" />
        <package android:name="cgg.gov.ghmc" />
        <package android:name="com.amplvb.ccrs" />
        <package android:name="com.nhai.rajmargyatra" />
        <package android:name="com.nammabengaluruNew.org" />
        <package android:name="com.esri.ugms_bmc" />
        <package android:name="in.gov.pmc.pmccare" />
        <package android:name="com.nic.dl.delhijanmitra" />
        <package android:name="in.nic.up.jansunwai.upjansunwai" />
        <package android:name="com.rajsampark.versiontwo" />
        <package android:name="in.gov.dpg.cmhelpline" />
        <package android:name="com.magnum.helpline" />
        <package android:name="com.bpsms.jansamadhan" />
        <package android:name="com.sociomatic.janasunani" />
        <package android:name="com.google.android.apps.maps" />
    </queries>

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:remove="android:requestLegacyExternalStorage">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- No video SEND/SEND_MULTIPLE filters while the page has no element that
                 shows a shared clip: Android would list the app in every video share
                 sheet, copy the clip into the cache and open a plain Home. Restore them
                 with the import card (see SHARE_INGRESS_ENABLED). Portrait because the
                 Drive HUD and the mount guidance are portrait; sideways, the count and
                 status fall below the fold. -->
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!-- Not a foreground service in this build. The native background Drive path
             that would promote it is unreachable (its toggle is hidden), and Play asks
             for a demonstration of every foreground service type an app declares. -->
        <service
            android:name=".drivemode.DriveModeService"
            android:exported="false" />
    </application>

    <!-- Permissions. Each one needs a user in the wired tree (check-native-contracts.mjs).
         CHANGE_NETWORK_STATE (the RTSP dashcam source) and POST_NOTIFICATIONS (the
         foreground Drive notification) come back with the code that uses them. -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

</manifest>
